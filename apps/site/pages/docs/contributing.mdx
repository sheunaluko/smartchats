import { Callout } from 'nextra/components'

# Contributing

The full contribution guide lives at the repo root:

**👉 [CONTRIBUTING.md on GitHub](https://github.com/sheunaluko/smartchats/blob/master/CONTRIBUTING.md)**

It covers the quick start, the architecture, the verification workflow,
the conventions, the PR workflow, and a section dedicated to **AI coding
agents** contributing to the codebase.

The TL;DR for new contributors:

1. **Clone, install, run `bin/test_all --quick`** — about 30 seconds; type-checks
   everything. If green, your environment is sound.
2. **Adding a new query the agent can run?** It's three files, ~20 lines
   total. See [the architecture doc](/docs/architecture#adding-a-new-query-the-agent-can-run).
3. **Run `bin/test_all` at the right level** for the change. The
   table in [CONTRIBUTING.md → Verification Workflow](https://github.com/sheunaluko/smartchats/blob/master/CONTRIBUTING.md#verification-workflow)
   shows which level matches which kind of change.

## Repo layout (high level)

```
smartchats/
├── apps/
│   └── smartchats/                # Next.js app (chat UI, voice, KG)
├── packages/
│   ├── smartchats-backend/        # DataAPI contract (types only)
│   ├── smartchats-backend-firebase/  # Cloud-target backend adapter
│   ├── smartchats-backend-local/     # Local-target backend adapter
│   ├── smartchats-cloud-client/   # Open client for the hosted cloud
│   ├── smartchats-cli/            # `smartchats` command
│   ├── smartchats-database/       # SurrealDB queries + operations + DataAPI factories
│   ├── smartchats-local-server/   # Express server backing the local stack
│   ├── smartchats-mcp/            # MCP server
│   └── ...
├── bin/
│   ├── aio                        # Run the all-in-one container
│   ├── cloud_test_db              # Local SurrealDB for cloud-shape testing
│   ├── devserve                   # Next.js dev + Firebase emulator
│   ├── test_all                   # The single-command test runner
│   └── ...
└── CONTRIBUTING.md                # The canonical guide
```

## Architecture invariants

These are load-bearing. Don't break them without discussion:

- **Single direct importer of `surrealdb`**: only
  `packages/smartchats-database/src/client.ts` imports the npm package.
  Every other consumer goes through the `Client` interface.
- **One credential store.** `~/.smartchats-mcp/credentials.json` shared
  by CLI + MCP. Don't invent a parallel auth file.
- **The `DataAPI` contract is the read/write boundary.** Don't add
  alternative dispatcher abstractions.
- **Per-statement errors must surface.** No silent `[]` on `status: ERR`.

See [Architecture → Open vs closed](/docs/architecture#open-vs-closed)
for the package-level reasoning.

## For AI coding agents

SmartChats is built collaboratively by humans and AI coding agents. The
same conventions apply to both — same quality bar, same review process,
same guide. This section captures what tends to make agent contributions
land well:

- **Read existing code before writing new code.** Most patterns you'll
  reach for already exist somewhere — search first, then write. The
  layered architecture means nearly every "I need to add X" maps to
  exactly one place.
- **Make small, surgical changes.** A feature should usually touch one
  builder, one tool wrapper, and one CLI command — not five files.
  When the diff sprawls, you're working against the architecture.
- **Run `bin/test_all` at the right level** before declaring done. The
  table in CONTRIBUTING.md picks the level. Don't rely on CI alone for
  your own change.
- **For destructive actions** (deleting data, force-pushing, modifying
  live cloud, killing shared processes), confirm with the user first.
  Reversibility is a one-way valve.
- **When tests fail, find the cause.** Don't blame "flaky environment"
  or "pre-existing issues." Read the log, reproduce the case, fix the
  root cause.
- **Track architecture-improvement ideas in `data/ARCHITECTURE_NOTES.md`**
  rather than refactoring as you go. The maintainer reviews these
  before they ship.
- **Match the existing tone.** Doc edits should sound like the
  surrounding text. Code edits should match the surrounding style.
  Comment density matches the file's norm.

<Callout type="info">
  Re-read this section at the start of each session. It's the fastest
  way to load project conventions and avoid the most common
  agent-contribution failure modes.
</Callout>

## Help / Discussion

- **GitHub issues** — [github.com/sheunaluko/smartchats/issues](https://github.com/sheunaluko/smartchats/issues)
- **LinkedIn (maintainer)** — [linkedin.com/in/sheun-aluko](https://www.linkedin.com/in/sheun-aluko/)

For larger architectural changes, open an issue with the `architecture`
label and tag the maintainer.

Thanks for contributing.
