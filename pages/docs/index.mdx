# SmartChats

**Voice-native, agentic, MCP-bidirectional. Open source.**

SmartChats is a platform for building voice-first AI agents that talk, remember,
search the web, write and run code, and integrate with everything else
through the Model Context Protocol.

The same agent works two ways:

- **Cloud** — sign up at [smartchats.ai](https://smartchats.ai), bring your
  own API keys, get billing, BYO usage caps, no infra to run.
- **Self-hosted** — run the all-in-one container on your laptop or a VPS.
  Same UI, same agent, same capabilities. Your data stays on your machine.

## Why this exists

The agent layer of an AI app — chat history, knowledge graph, executed code,
session state — is currently siloed in proprietary apps that you can't extend,
inspect, or swap out. SmartChats is the layer that sits underneath any voice
or chat agent: a programmable runtime with stable interfaces, an open data
model, and the same code path whether you're talking to it from a browser, a
CLI, or another LLM via MCP.

The pitch in one diagram:

```
    you, the user            another LLM (Claude Desktop, etc.)
         │                              │
         ▼                              ▼
   smartchats app                 smartchats-mcp
         │                              │
         └──────────────┬───────────────┘
                        ▼
        ┌──────────────────────────────────┐
        │   Operations + Query Builders    │   shared core — same code
        │   (importBundle, searchLogs,     │   every consumer uses
        │    queryKnowledgeGraph, ...)     │
        └──────────────────────────────────┘
                        ▼
              DataAPI contract
                ╱            ╲
         Cloud target      Local target
       (Firebase Auth +    (SDK-direct
        Cloud Functions     to local DB,
        + per-user JWT)     root creds)
```

If you're contributing a feature: add a query builder once, both surfaces
get it. If you're consuming SmartChats: pick whichever surface fits — the
voice app, the CLI, the MCP server — they all see the same data.

## Get started

import { Cards } from 'nextra/components'

<Cards>
  <Cards.Card title="Quickstart →" href="/docs/quickstart" arrow>
    Five minutes from clone to first conversation.
  </Cards.Card>
  <Cards.Card title="Architecture →" href="/docs/architecture" arrow>
    The layered model + facade pattern + shared core that makes
    extending SmartChats one-line.
  </Cards.Card>
  <Cards.Card title="CLI Reference →" href="/docs/cli" arrow>
    `smartchats login`, `data import|export`, `launch` — every flag and example.
  </Cards.Card>
  <Cards.Card title="MCP Server →" href="/docs/mcp" arrow>
    Wire SmartChats into Claude Desktop or any MCP-aware LLM.
  </Cards.Card>
</Cards>

## What's in the stack

- **The chat / voice app** — Next.js, written in TypeScript, runs in your browser.
  Voice in via VAD + streaming ASR; voice out via streaming TTS; agent loop in
  between with structured-output streaming, sandboxed code execution, embedded
  knowledge graph.
- **The CLI** (`smartchats`) — manages auth, exports/imports your data between
  cloud and local, launches the all-in-one stack.
- **The MCP server** (`smartchats-mcp`) — speaks Model Context Protocol over
  stdio so any MCP-aware LLM (Claude Desktop, etc.) can read and search your
  SmartChats data.
- **The local-server** + **AIO container** — single docker image with
  SurrealDB + Express + the Next.js app. `bin/aio` and you're running.

All of it is open source.

## Help

- [GitHub issues](https://github.com/sheunaluko/tidyscripts/issues) for bugs and feature requests.
- [LinkedIn (maintainer)](https://www.linkedin.com/in/sheun-aluko/) for design discussions.
- [Contributing guide →](/docs/contributing) for the conventions, the agent-onboarding section, and the dead-simple workflow.
