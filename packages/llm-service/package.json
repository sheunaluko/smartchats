{
  "name": "llm-service",
  "version": "0.1.0",
  "description": "Provider-agnostic LLM request + streaming handlers for OpenAI, Anthropic, Gemini, and xAI. Normalizes responses into a common shape and supports structured output, streaming, combined LLM+TTS NDJSON orchestration with a TTS adapter registry (OpenAI + Azure), and BYO API keys.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@google/genai": "^1.42.0",
    "cortex": "*",
    "js-tiktoken": "^1.0.13",
    "microsoft-cognitiveservices-speech-sdk": "^1.41.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  },
  "license": "Apache-2.0"
}
