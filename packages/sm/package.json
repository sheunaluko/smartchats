{
    "name": "sm",
    "version": "0.0.1",
    "private": true,
    "description": "Maintainer CLI for smartchats — unified verify / dev / deploy / ship surface across open + cloud repos.",
    "type": "module",
    "main": "dist/cli.js",
    "bin": {
        "sm": "dist/cli.js"
    },
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist",
        "type-check": "tsc --noEmit",
        "dev": "tsx src/cli.ts"
    },
    "dependencies": {
        "consola": "^3.2.3"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "tsx": "^4.7.0",
        "typescript": "^5.4.5"
    }
}
